import styled from "styled-components";
import UIToken from "../../../utils/UIToken";

const H1 = styled.h1`
	color: inherit;
	font-size: ${UIToken.fontSizeLg};
	padding: ${UIToken.spacingLg}px 0;
	margin: 0;
`;

export default H1;
